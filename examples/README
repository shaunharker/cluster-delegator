
1. Build examples by typing (from within this directory)

../.install/build.sh

2. Test examples by typing

mpiexec -np 4 ./bin/example1

and 

mpiexec -np 4 ./bin/example2 42 is the answer

3. Take a look at the code: 
   example1.cpp
   example2.cpp
